/**
 * CONTEXT API
 * 1.Create context and export
 * 2.create provider
 * 3.set context value
 * 4.to access the value: useContext hook
 *
 *
 * TO use
 * 1.set AuthProvider as children in main.jsx
 * 2.inside the Provider get the children as props and use
 */
